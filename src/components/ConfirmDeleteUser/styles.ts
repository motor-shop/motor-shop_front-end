import styled from "styled-components";

export const ConfirmDeleteUserStyled = styled.div`
  h3 {
    font-family: "Lexend";
    font-weight: 500;
    font-size: 16px;
    line-height: 20px;
    color: var(--color-gray-0);
  }

  p {
    font-family: "Inter";
    font-weight: 400;
    font-size: 16px;
    line-height: 28px;
    color: var(--color-gray-2);
  }

  div {
    margin-top: 43px;
    height: 48px;
    display: flex;
    justify-content: flex-end;
    gap: 10px;
  }
`;
